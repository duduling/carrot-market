function countRegexMatches(target: string | null, regexString: string): number {
  if (typeof target === "/* implement Answer 1 */") {
    let result = target["/* implement Answer 2 */"](
      new RegExp(regexString, "g")
    );
    if (result !== null) {
      return /* implement Answer 3 */;
    }
  }
  return 0;
}
